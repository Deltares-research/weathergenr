CONTEXT
You are modifying an R package named weathergenr.

GOAL
Improve correctness, readability, maintainability, and testability.
Changes must be minimal and surgical.

HARD CONSTRAINTS (non-negotiable)
Keep all files UTF-8.
Do not change exported function names, arguments, or return structures unless explicitly instructed.
Do not add new external dependencies; prefer base R and existing Imports. If unavoidable, justify briefly.
Do not introduce purrr or functional-programming rewrites; use explicit for loops.
Preserve deterministic behavior (control seeds/randomness where relevant).
Do not reformat entire files or apply style-only sweeps.
Do not change user-visible outputs unless explicitly requested.
Do not silently drop columns or fields.

NON-INTERACTIVE EXECUTION (mandatory)
Do not ask questions or request confirmation.
Do not provide interim progress updates.
Execute end-to-end in a single pass.
If information is missing or ambiguous, apply the smallest safe assumption consistent with package conventions.
Iterate until relevant tests pass, or only a clearly external/irreproducible failure remains; report that failure only in the final summary.
Stop only when all requested changes are implemented and tests have been updated and run.

PACKAGE CONVENTIONS (weathergenr)
Internal helpers: .helper_name()
User-facing functions: snake_case
Argument names: snake_case, descriptive
User-facing errors: stop(..., call. = FALSE)
Use warning() sparingly; use logger only if already present in the file
Qualify non-imported base packages (stats::, utils::, etc.)

STYLE
Prefer guard clauses and early returns.
Use explicit type/length checks; validate scalars with existing helpers (e.g., .is_int_scalar()).
Avoid deep nesting; extract small helpers only when it improves readability.
Naming consistency: obs_*, sim_*, cfg, out, idx, n_*, is_*.

DOCUMENTATION
Exported functions: update roxygen2 to match behavior exactly.
@description: single paragraph
@details: key assumptions and edge cases (brief)
@param: precise types/dimensions/units; NA handling
@return: exact structure/class/fields
@examples: minimal and fast; wrap expensive code in \dontrun{}
Internal functions: minimal docs, @keywords internal only if needed.
Do not invent parameters or behavior.

TESTS
Add or adjust testthat tests only when behavior changes or gaps exist.
Tests must be deterministic; avoid network, large fixtures, or uncontrolled randomness.
Cover: input validation, NA handling, empty/constant series, methodological consistency, return structure.
Group tests by function or function group.
At the top of each test file, list functions under test with relative paths.

FINAL OUTPUT (once complete)
- Bullet summary:
  - what changed
  - why it changed
  - risks and assumptions
- List of files changed.
- When asked for a review, provide your findings in a table format with a 
    Issue ID, description, priority and location (e.g., R/file.R lines XXX:YYY)
    Always indicate the type of the issue, whether methodological, technical, or efficiency related. 
- Explicit statement of any design choices or assumptions made.
- Report any remaining check or test issues only if they are external
  or irreproducible.

TASKS
- Check the status of the issues below and report me back: not implemented, partially implemented, or fully implemented.  
- Next, ask me which ones to fix and get my approval to fix them:


| ID | Issue | Severity | Location |
|----|-------|----------|----------|
| E1 | **Per-year `split()` + `unlist()` in resampling loop** | Medium | `resample.R:343-344` |
| | `split(seq_along(obs_wyear), obs_wyear)` and `unlist(obs_idx_by_year[...])` are called inside the yearly loop. Since `obs_wyear` doesn't change across years, the `split()` can be hoisted outside the `for (y in ...)` loop. The `unlist()` of selected years changes per year but the base lookup table should be precomputed. | | |
| E2 | **Per-year `tapply()` calls** | Medium | `resample.R:359-380` |
| | Monthly means, SDs, and thresholds are computed via `tapply()` inside each year's loop body. These operate on the year-specific subset, which does change per year (because `obs_subset_idx` changes). This is correct but expensive. If `annual_knn_n` draws many of the same years repeatedly, caching per-year statistics would help. | | |
| E3 | **Daily KNN seed per time step** | Medium | `resample.R:588` |
| | `seed = base_seed + t_sim` calls `set.seed()` once per simulated day inside the inner loop. `set.seed()` involves system calls and is non-trivial overhead when called ~13,000 times per realization (365 * ~36 years). Consider pre-generating the required random draws or seeding only per year. | | |
| E4 | **CWT analysis per simulated realization in `filter_warm_pool`** | High | `warm_filtering.R` |
| | Spectral metrics require a CWT analysis for each of the `warm_pool_size` simulated series. With `warm_pool_size = 5000`, this is the dominant cost. The parallel option helps, but consider a fast GWS approximation (e.g., periodogram-based) for initial screening, with full CWT only for candidates passing distributional filters. | | |
| E5 | **Covariance matrix in `modwt_mra()`** | Low | `wavelet_modwt.R:403` |
| | `stats::cov(components)` computes a full `J x J` covariance matrix every call. For `J <= 5` this is negligible, but the same computation happens again in `analyze_wavelet_additive()` line 610. Deduplicate by passing through. | | |
| E6 | **`variance_match_matrix` triple sweep** | Low | `wavelet_warm.R:776-778` |
| | Three `sweep()` calls on potentially large matrices (n x n_sim). These could be combined into a single operation: `sim2[, do_fix] <- (sim2[, do_fix] - sim_mean[do_fix]) * scale[do_fix] + target_mean`. Using column-wise vectorization with `rep()` and matrix arithmetic would avoid the overhead of three separate `sweep()` calls. | | |
| E7 | **Memory: full innovation matrix in `.simulate_arma_matrix()`** | Low | `wavelet_warm.R:705-709` |
| | The innovation matrix `eps` has dimensions `(n_tot + max_lag) x n_sim`. For `n = 50, n_sim = 5000, burnin = 100`, this is ~750K doubles (~6 MB). Manageable, but if `n_sim` grows to 50,000 this becomes 60 MB. Not a concern at current scale. | | |


 DONE CRITERIA
- New tests pass.
- Existing tests pass.
- No change to exported function signatures or return structures.


