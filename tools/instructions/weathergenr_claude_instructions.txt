PACKAGE MODIFICATION INSTRUCTIONS (weathergenr)

CONTEXT
You are modifying an R package named weathergenr.

GOAL
Improve correctness, readability, maintainability, numerical stability, and testability.
All changes must be minimal, surgical, and justified.

HARD CONSTRAINTS (NON-NEGOTIABLE)

- Encoding: All files must remain UTF-8.
- Public API stability:
  Do not change exported function names, arguments, defaults, return types,
  column names, or classes unless explicitly instructed.
- Numerical compatibility:
  Preserve numerical outputs within tolerance 1e-12 unless the task explicitly
  changes methodology.
- Dependencies:
  Do not add new external dependencies.
  Do not introduce runtime reliance on packages in Suggests.
  If unavoidable, guard with requireNamespace() and justify explicitly.
- Programming style:
  Do not introduce purrr or functional-programming rewrites.
  Use explicit loops and base R idioms.
- Determinism:
  Preserve deterministic behavior.
  Control randomness and seeds explicitly where relevant.
- Minimal diff rule:
  Modify only what is necessary.
  Do not rename or move files unless explicitly instructed.
  Do not apply style-only or sweeping refactors.
- User-visible behavior:
  Do not silently change outputs, warnings, errors, or messages.
  Any required change must be documented in the final summary.
- Data integrity:
  Do not silently drop columns, fields, or rows.

NON-INTERACTIVE EXECUTION (MANDATORY)

- Execute end-to-end in a single pass.
- Do not ask questions or request confirmation.
- Do not provide interim progress updates.
- If information is ambiguous, apply the smallest conservative assumption
  consistent with package conventions.
- Iterate until relevant tests pass or only a clearly external or irreproducible
  failure remains.
- Stop only when all requested changes are implemented and tests are updated and run.

STYLE & STRUCTURE

- Organize functions within each script by functional responsibility.
- At the top of each script, list:
  - functions under test
  - relative paths
  - one-line purpose
- Prefer guard clauses and early returns.
- Use explicit type/length checks; validate scalars with existing helpers
  (e.g., .is_int_scalar()).
- Avoid deep nesting; extract helpers only when readability materially improves.
- Avoid asymptotic regressions; preallocate and avoid O(n²) patterns on time-series data.

NUMERICAL & METHODOLOGICAL SAFETY

- Handle NA / NaN / Inf explicitly; never rely on implicit propagation.
- Make thresholding, scaling, and aggregation assumptions explicit and documented.
- Do not change statistical definitions unless explicitly instructed.
- When baseline values are zero or undefined, use conservative,
  well-documented handling.
- Avoid locale- or timezone-dependent behavior; use explicit parsing and TZ = UTC
  where relevant.

DOCUMENTATION

- Exported functions:
  Update roxygen2 to match behavior exactly.
  - @description: single, precise paragraph
  - @details: key assumptions and edge cases (brief)
  - @param: precise types, dimensions, units, NA handling
  - @return: exact structure, classes, and fields
  - @examples: minimal and fast; wrap expensive code in \dontrun{}
- Internal functions:
  Minimal documentation; use @keywords internal only when necessary.
- Do not invent parameters, defaults, or behavior.

TESTS

- Add or update testthat tests whenever behavior or assumptions change.
- Tests must be deterministic.
- Avoid network access, large fixtures, or uncontrolled randomness.
- Explicitly cover:
  - input validation
  - NA / empty / constant series
  - methodological consistency
  - return structure (class, names, dimensions)
- Group tests by function or function group.
- At the top of each test file, list functions under test with relative paths.
- New parameters require:
  - one test for default behavior
  - one test for a non-default path

FINAL OUTPUT (ONCE COMPLETE)

- Bullet summary:
  - what changed
  - why it changed
  - risks and assumptions
- List of files changed.
- Explicit statement of any design choices or assumptions made.
- Report any remaining check or test issues only if they are external
  or irreproducible.
