% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_generator.R
\name{.year_keep_index}
\alias{.year_keep_index}
\title{Row index selecting the rows of a shared date vector falling in given years}
\usage{
.year_keep_index(dates, years_keep, year_start_month = 1L)
}
\arguments{
\item{dates}{Date vector shared by every grid on this side.}

\item{years_keep}{Integer vector of years (calendar or water years) to retain.}

\item{year_start_month}{Integer 1-12. When > 1, uses water-year grouping.}
}
\value{
Logical vector, length `length(dates)`.
}
\description{
Every grid within `daily_obs` (and within one realization of `daily_sim`)
shares a single date vector, so the row index is computed once here and
reused across grids rather than recomputing the year vector per grid. With
25 grids and 3 realizations that is one call instead of 100.
}
\keyword{internal}
