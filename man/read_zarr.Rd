% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_zarr.R
\name{read_zarr}
\alias{read_zarr}
\title{Read Zarr variables into tidy data frames}
\usage{
read_zarr(
  dir,
  var = NULL,
  var_name = NULL,
  keep_leap_day = TRUE,
  drop_all_na = TRUE,
  spatial_ref = "spatial_ref",
  signif_digits = NULL,
  slice_mode = 1,
  verbose = FALSE
)
}
\arguments{
\item{dir}{Character. Path to Zarr store (directory).}

\item{var}{Character vector or NULL. Variables to read. If NULL, reads all
non-coordinate variables (and excludes `spatial_ref` if present).}

\item{var_name}{NULL, or named character vector.
- If named: names are original variable names and values are new names.
Renaming applies to output list names and the value-column name.}

\item{keep_leap_day}{Logical. If FALSE and time is convertible to Date, removes Feb 29 rows.}

\item{drop_all_na}{Logical. If TRUE, drop variables whose values are all NA
after applying _FillValue/missing_value handling.}

\item{spatial_ref}{Character. Name of a spatial reference variable to ignore
when auto-selecting variables (common in CF/CRS-encoded files).}

\item{signif_digits}{Integer or NULL. If provided, rounds numeric values via
signif(x, signif_digits).}

\item{slice_mode}{Integer. Indexing mode for pizzarr: 1 for R-style (1-based),
0 for Python-style (0-based). Default is 1.}

\item{verbose}{Logical. If TRUE, prints basic progress messages.}
}
\value{
List with components:
  \item{data}{List of data.frames (one per grid cell)}
  \item{grid}{Tibble with id, xind, yind, x, y columns}
  \item{date}{Vector of Date values}
  \item{dimensions}{List of dimension coordinate vectors}
  \item{attributes}{List with global and variable attributes}
}
\description{
Reads one or more variables from a Zarr store and returns a structured list
containing data, grid, date, dimensions, and attributes - matching the output
structure of read_netcdf().
}
