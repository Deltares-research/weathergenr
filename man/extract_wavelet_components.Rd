% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wavelet.R
\name{extract_wavelet_components}
\alias{extract_wavelet_components}
\title{Reconstruct Wavelet Components from Selected Scales}
\usage{
extract_wavelet_components(
  wave,
  signif_periods,
  scale,
  dj = 0.25,
  dt = 1,
  series_sd,
  series_mean = 0,
  Cdelta = 0.776,
  w0_0 = pi^(-1/4),
  include_residual = TRUE
)
}
\arguments{
\item{wave}{Complex matrix (scales x time). Wavelet coefficients.}

\item{signif_periods}{Integer vector. Scale indices to reconstruct.}

\item{scale}{Numeric vector. Scale values aligned with \code{nrow(wave)}.}

\item{dj}{Numeric scalar. Scale resolution used in the transform.}

\item{dt}{Numeric scalar. Time step of the series.}

\item{series_sd}{Numeric scalar. Standard deviation of the original series (pre-standardization).}

\item{series_mean}{Numeric scalar. Mean of the original series; added back to residual.}

\item{Cdelta}{Numeric scalar. Morlet reconstruction constant (default: 0.776).}

\item{w0_0}{Numeric scalar. \eqn{\psi_0(0)} constant (default: \eqn{\pi^{-1/4}}).}

\item{include_residual}{Logical. If \code{TRUE}, adds a residual (non-selected) component.}
}
\value{
Numeric matrix with one column per selected component and, if requested,
  a final \code{"Noise"} residual column.
}
\description{
Reconstructs time-domain components from selected wavelet scales using the
Torrence & Compo (1998) inverse transform approximation. Optionally adds a residual
component representing non-selected scales to ensure additive closure.
}
\keyword{internal}
