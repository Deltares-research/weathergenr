% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile_mapping.R
\name{fit_monthly_distributions}
\alias{fit_monthly_distributions}
\title{Fit Monthly Gamma Distributions for Wet-Day Precipitation}
\usage{
fit_monthly_distributions(prcp_wet, month_wet, months_ok, fit_method, verbose)
}
\arguments{
\item{prcp_wet}{Numeric vector of wet-day precipitation values (strictly > 0
recommended). Length must match `month_wet`.}

\item{month_wet}{Integer vector of months (1-12) corresponding to each element
of `prcp_wet`.}

\item{months_ok}{Integer vector of months to attempt fitting (subset of 1-12).}

\item{fit_method}{Character scalar. Estimation method forwarded to
`fitdistrplus::fitdist()` (e.g., `"mle"`, `"mme"`).}

\item{verbose}{Logical. If `TRUE`, emits a warning when a monthly fit fails.}
}
\value{
A `data.frame` with columns:
\itemize{
  \item `month` Month number.
  \item `shape` Gamma shape parameter.
  \item `scale` Gamma scale parameter.
  \item `mean` Implied mean (`shape * scale`).
  \item `var` Implied variance (`shape * scale^2`).
}
Months with failed fits are removed. The returned data frame has an attribute
`n_failed_fits` with the number of failed months.
}
\description{
Fits a gamma distribution to wet-day precipitation amounts for each month in
`months_ok`. Uses `fitdistrplus::fitdist()` and returns month-specific gamma
parameters and implied moments. Months that fail to fit are dropped.
}
\keyword{internal}
