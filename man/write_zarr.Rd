% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_zarr.R
\name{write_zarr}
\alias{write_zarr}
\title{Write tidy data frames to Zarr store}
\usage{
write_zarr(
  dir,
  data = NULL,
  grid = NULL,
  date = NULL,
  dimensions = NULL,
  attributes = NULL,
  input_list = NULL,
  x_dim_name = "lon",
  y_dim_name = "lat",
  time_dim_name = "time",
  time_units = NULL,
  fill_value = -9999,
  overwrite = FALSE,
  compression = "zlib",
  compression_level = 5,
  chunk_size = NULL,
  slice_mode = 1,
  verbose = FALSE
)
}
\arguments{
\item{dir}{Character. Path to output Zarr store (will be created).}

\item{data}{List of data.frames (one per grid cell), each with time-series
columns for variables. OR a single data.frame with coordinates.}

\item{grid}{Tibble/data.frame with columns: id, xind, yind, x, y.}

\item{date}{Vector of Date or POSIXct values for the time dimension.}

\item{dimensions}{Named list of dimension coordinate vectors (x, y, time).}

\item{attributes}{List with $global and $variables sub-lists for metadata.}

\item{input_list}{Alternative: a single list with components data, grid, date,
dimensions, attributes (as returned by read_netcdf/read_zarr).}

\item{x_dim_name}{Character. Name for x/longitude dimension (default: "lon").}

\item{y_dim_name}{Character. Name for y/latitude dimension (default: "lat").}

\item{time_dim_name}{Character. Name for time dimension (default: "time").}

\item{time_units}{Character. CF-compliant time units (e.g., "days since 1850-01-01").
If NULL and date is Date, uses "days since YYYY-01-01" from first date.}

\item{fill_value}{Numeric. Value to use for missing data (default: -9999).}

\item{overwrite}{Logical. If TRUE, overwrite existing zarr store (default: FALSE).}

\item{compression}{Character. Compression algorithm: "none", "zlib", "blosc" (default: "zlib").}

\item{compression_level}{Integer. Compression level 0-9 (default: 5).}

\item{chunk_size}{Named list. Chunk dimensions (e.g., list(time=365, lat=50, lon=50)).
If NULL, uses automatic chunking.}

\item{slice_mode}{Integer. Indexing mode for pizzarr: 1 for R-style (1-based),
0 for Python-style (0-based). Default is 1.}

\item{verbose}{Logical. If TRUE, print progress messages.}
}
\value{
Invisibly returns the path to the created zarr store.
}
\description{
Writes gridded time-series data to a Zarr store. Can accept either a
structured list (e.g., from read_netcdf/read_zarr) or individual components.
}
