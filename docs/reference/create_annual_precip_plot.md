# Create annual precipitation plot

Create annual precipitation plot

## Usage

``` r
create_annual_precip_plot(
  stats.annual.aavg.sim,
  stats.annual.aavg.obs,
  plot.config,
  show.title,
  save.plots,
  output.path
)
```
