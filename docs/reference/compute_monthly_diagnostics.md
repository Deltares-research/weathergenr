# Compute month-by-month diagnostics for quantile-mapped precipitation

Compute month-by-month diagnostics for quantile-mapped precipitation

## Usage

``` r
compute_monthly_diagnostics(
  prcp_ref,
  prcp_adj,
  month,
  mean_factor = NULL,
  var_factor = NULL,
  year = NULL
)
```
