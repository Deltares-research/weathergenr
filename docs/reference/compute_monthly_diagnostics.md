# Compute monthly diagnostics

Compute monthly diagnostics

## Usage

``` r
compute_monthly_diagnostics(
  value.original,
  value.adjusted,
  mon.ts,
  mean.change = NULL,
  var.change = NULL,
  year.ts = NULL
)
```
