# Log info message with FILTERING prefix

Wrapper for logger::log_info with fallback to message(). Automatically
adds \[FILTERING\] prefix to all messages.

## Usage

``` r
log_info(txt)
```

## Arguments

- txt:

  Character message
