# Filter correlations by type (cross-grid or inter-variable)

Filter correlations by type (cross-grid or inter-variable)

## Usage

``` r
filter_correlations(cor_data, same_var, same_id, allowed_pairs, pair_type)
```
