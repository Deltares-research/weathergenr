# Compute target distribution parameters from base params and change factors

Compute target distribution parameters from base params and change
factors

## Usage

``` r
compute_target_parameters(
  base_gamma,
  mean_factor,
  var_factor,
  months_ok,
  n_years
)
```
