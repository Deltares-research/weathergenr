# Format Elapsed Time for Display

Computes elapsed time from a start time and formats it as a
human-readable string with appropriate units (seconds, minutes, or
hours).

## Usage

``` r
format_elapsed(start_time)
```

## Arguments

- start_time:

  POSIXct object. The start time to measure from.

## Value

Character string with formatted elapsed time.
