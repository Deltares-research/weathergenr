# Create all diagnostic plots

Create all diagnostic plots

## Usage

``` r
create_all_diagnostic_plots(
  plot.data,
  plot.config,
  variables,
  variable.labels,
  show.title,
  save.plots,
  output.path
)
```
