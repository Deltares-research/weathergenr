# Generate symmetric dummy points for equal axis scaling

Generate symmetric dummy points for equal axis scaling

## Usage

``` r
generate_symmetric_dummy_points(
  df,
  facet.var = "variable",
  x.col = "Observed",
  y.col = "Simulated"
)
```
