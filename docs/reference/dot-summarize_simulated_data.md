# Summarize simulated weather data

Summarize simulated weather data

## Usage

``` r
.summarize_simulated_data(
  daily_sim,
  n_realizations,
  variables,
  mc_thresholds,
  year_start_month = 1L,
  parallel = FALSE,
  n_cores = NULL,
  seed = NULL
)
```
