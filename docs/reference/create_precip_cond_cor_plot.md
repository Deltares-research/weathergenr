# Create inter-grid correlation plot conditional on precipitation

Create inter-grid correlation plot conditional on precipitation

## Usage

``` r
create_precip_cond_cor_plot(
  stats.precip_cor_cond,
  plot.config,
  show.title,
  save.plots,
  output.path
)
```
