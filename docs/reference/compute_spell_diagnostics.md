# Compute wet and dry spell-length diagnostics (intended preserved)

Compute wet and dry spell-length diagnostics (intended preserved)

## Usage

``` r
compute_spell_diagnostics(
  precip_ref,
  precip_adj,
  wet_thresh,
  tol_mean_ratio = 0.2
)
```
