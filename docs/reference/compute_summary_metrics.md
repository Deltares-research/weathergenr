# Compute summary metrics (no qualitative grades; no scoring)

Compute summary metrics (no qualitative grades; no scoring)

## Usage

``` r
compute_summary_metrics(
  moments,
  quant_metrics,
  extreme_metrics,
  spell_metrics,
  dryday_metrics
)
```
