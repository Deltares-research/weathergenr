# Compute extreme-value diagnostics for quantile-mapped precipitation

Compute extreme-value diagnostics for quantile-mapped precipitation

## Usage

``` r
compute_extreme_diagnostics(prcp_ref, prcp_adj)
```
