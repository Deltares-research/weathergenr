# Standardize obs and sim to same full-year length via random windowing Applies the chosen window consistently across all grids and all realizations.

Standardize obs and sim to same full-year length via random windowing
Applies the chosen window consistently across all grids and all
realizations.

## Usage

``` r
standardize_obs_sim_periods(daily.obs, daily.sim, realization.num, variables)
```
