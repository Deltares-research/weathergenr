# Pick a random contiguous window of years of length n_years from a year vector

Pick a random contiguous window of years of length n_years from a year
vector

## Usage

``` r
pick_year_window(years, n_years)
```
