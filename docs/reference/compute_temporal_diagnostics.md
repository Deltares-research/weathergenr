# Compute temporal diagnostics for quantile-mapped precipitation

Compute temporal diagnostics for quantile-mapped precipitation

## Usage

``` r
compute_temporal_diagnostics(prcp_ref, prcp_adj, month, year, mask)
```
