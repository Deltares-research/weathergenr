# Compute temporal diagnostics (no subjective categories)

Compute temporal diagnostics (no subjective categories)

## Usage

``` r
compute_temporal_diagnostics(precip_ref, precip_adj, month, year, mask)
```
