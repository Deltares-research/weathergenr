# Create monthly pattern plot for a single variable

Create monthly pattern plot for a single variable

## Usage

``` r
create_monthly_pattern_plot(
  stats.mon.aavg.sim,
  stats.mon.aavg.obs,
  variable,
  variable.label,
  plot.config,
  show.title,
  save.plots,
  output.path
)
```
