# Print method for additive wavelet analysis

Prints a compact summary of a `"wavelet_additive"` object.

## Usage

``` r
# S3 method for class 'wavelet_additive'
print(x, ...)
```

## Arguments

- x:

  Object of class `"wavelet_additive"`.

- ...:

  Ignored.

## Value

Invisibly returns `x`.
