# Compute quantile diagnostics for quantile-mapped precipitation

Compute quantile diagnostics for quantile-mapped precipitation

## Usage

``` r
compute_quantile_diagnostics(prcp_ref, prcp_adj, probs)
```
