# Validate inputs for weather assessment

Validate inputs for weather assessment

## Usage

``` r
.validate_assessment_inputs(
  daily_sim,
  daily_obs,
  vars,
  n_realizations,
  wet_q,
  extreme_q,
  verbose,
  parallel,
  n_cores
)
```
