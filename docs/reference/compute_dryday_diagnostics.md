# Compute dry-day and wet-day frequency diagnostics (intended preserved)

Compute dry-day and wet-day frequency diagnostics (intended preserved)

## Usage

``` r
compute_dryday_diagnostics(
  precip_ref,
  precip_adj,
  wet_thresh,
  tol_freq_abs = 0.01
)
```
