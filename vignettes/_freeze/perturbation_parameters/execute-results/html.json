{
  "hash": "cf733d11c85e3c0327e1efab6df14a37",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Precipitation Perturbation Parameters\"\ndescription: \"How the precipitation perturbation arguments change the outcome, measured against a common baseline on one stylized site.\"\nformat:\n  html:\n    toc: true\n    toc-location: left\n    toc-title: \"Contents\"\n    toc-depth: 3\n    toc-expand: 2\n    page-layout: full\n    number-sections: true\n    code-tools: true\n    df-print: tibble\n    self-contained: true\nexecute:\n  echo: true\n  warning: false\n  message: false\n  cache: false\n  freeze: true\n  execute-dir: project\nvignette: >\n  %\\VignetteIndexEntry{Precipitation Perturbation Parameters}\n  %\\VignetteEngine{quarto::html}\n  %\\VignetteEncoding{UTF-8}\n---\n\n# Purpose\n\n[Climate Perturbations](climate_perturbation.html) shows *how* to apply a\nperturbation.\nThis article asks a different question: given a target change, what do the\nremaining arguments actually do to the result?\n\nEvery scenario below imposes the same headline change — a 20% reduction in\nwet-day precipitation — and varies one argument at a time.\nHolding the target fixed is the point: differences between panels are the\nargument's doing, not the scenario's.\n\nThe site is stylized rather than real, so the mechanisms are visible without a\nregion's idiosyncrasies on top.\nEverything runs in a few seconds and no weather-generator call is involved.\n\n::: {.callout-note}\n## What is not covered\n`compute_pet` and `pet_method` are driven by temperature, not precipitation, and\nare covered in [Climate Perturbations](climate_perturbation.html).\n`precip_cap_mm_day`, `precip_floor_mm_day` and `precip_cap_quantile` are safety\nrails applied after everything here, and do nothing until a bound is hit.\n:::\n\n# Setup\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(weathergenr)\nlibrary(ggplot2)\nlibrary(dplyr)\nlibrary(tidyr)\n\ntheme_set(theme_weathergenr())\n```\n:::\n\n\n## A stylized site\n\nThirty years of daily precipitation on a 365-day calendar, with a wet season\nfrom November to March, gamma-distributed wet-day amounts, and a slow\ninterannual oscillation.\nThat last ingredient matters: without year-to-year variability, one of the\narguments below has nothing to act on and looks inert when it is not.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn_years <- 30L\nstart_year <- 2000L\n\ndate <- as.Date(\"2000-01-01\") + 0:(365L * n_years - 1L)\nmonth <- as.integer(format(date, \"%m\"))\nyear_idx <- as.integer(format(date, \"%Y\")) - start_year + 1L\n\nwet_prob <- c(0.55, 0.50, 0.45, 0.25, 0.15, 0.10,\n              0.08, 0.10, 0.18, 0.30, 0.45, 0.55)[month]\nwet_scale <- c(9, 8.5, 7.5, 5, 3.5, 2.5,\n               2, 2.5, 4, 6, 8, 9)[month]\n\n# A slow wet/dry oscillation, so the series has interannual structure.\nann_mult <- 1 + 0.35 * sin(2 * pi * seq_len(n_years) / 11)\n\nset.seed(20260817)\nprecip <- numeric(length(date))\nis_wet <- runif(length(date)) < wet_prob * ann_mult[year_idx]\nprecip[is_wet] <- rgamma(\n  sum(is_wet),\n  shape = 0.75,\n  scale = wet_scale[is_wet] * ann_mult[year_idx][is_wet]\n)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(\n  days           = length(precip),\n  wet_days       = sum(precip > 0),\n  wet_fraction   = mean(precip > 0),\n  annual_mm      = mean(tapply(precip, year_idx, sum)),\n  wetday_mean_mm = mean(precip[precip > 0])\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 5\n   days wet_days wet_fraction annual_mm wetday_mean_mm\n  <int>    <int>        <dbl>     <dbl>          <dbl>\n1 10950     3335        0.305      629.           5.66\n```\n\n\n:::\n:::\n\n\n## Instruments and metrics\n\nThe shape arguments belong to `adjust_precipitation_qm()`, the quantile mapper\nthat `apply_climate_perturbations()` calls internally.\nGoing straight to it keeps the grid and temperature scaffolding out of the way.\nThe two `*_transient` arguments exist only on the wrapper, so\n@sec-transient switches instruments.\n\nNote the one interface difference: the wrapper accepts a length-12 vector and\nexpands it, while the mapper requires an `n_years x 12` matrix.\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# The mapper wants a matrix; expand a monthly vector to one.\nas_factor_matrix <- function(v) {\n  matrix(v, nrow = n_years, ncol = 12, byrow = TRUE)\n}\n\nperturb <- function(mean_factor = 0.8, var_factor = 1, ...) {\n  adjust_precipitation_qm(\n    precip      = precip,\n    month       = month,\n    year        = year_idx,\n    mean_factor = as_factor_matrix(rep(mean_factor, 12)),\n    var_factor  = as_factor_matrix(rep(var_factor, 12)),\n    validate_output = FALSE,\n    diagnostics     = FALSE,\n    verbose         = FALSE,\n    ...\n  )\n}\n```\n:::\n\n\nFour metrics, chosen so the arguments separate rather than overlap.\nThe last two are the ones that matter most, and pooling them — as a single\noverall coefficient of variation would — hides exactly the distinction\n@sec-two-knobs draws.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nwet_of <- function(x) x[x > 0]\n\n# Groups are (month, year), the unit the perturbation itself works on.\nby_group <- function(x) {\n  w <- x > 0\n  g <- split(x[w], list(month[w], year_idx[w]), drop = TRUE)\n  g[lengths(g) >= 5]\n}\n\nsummarise_series <- function(x, label) {\n  g <- by_group(x)\n  group_means <- vapply(g, mean, numeric(1))\n  tibble(\n    scenario    = label,\n    wetday_mean = mean(wet_of(x)),\n    p99         = unname(quantile(wet_of(x), 0.99)),\n    # Spread of daily values inside a (month, year) group, averaged.\n    within_cv   = mean(vapply(g, function(v) sd(v) / mean(v), numeric(1))),\n    # Spread of the group means themselves: the interannual signal.\n    between_cv  = sd(group_means) / mean(group_means)\n  )\n}\n```\n:::\n\n\nThe baseline every scenario is measured against applies the 20% reduction with\nevery other argument at its default.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbaseline <- perturb()\n\nbind_rows(\n  summarise_series(precip, \"observed\"),\n  summarise_series(baseline, \"baseline (-20%, defaults)\")\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 5\n  scenario                  wetday_mean   p99 within_cv between_cv\n  <chr>                           <dbl> <dbl>     <dbl>      <dbl>\n1 observed                         5.66  36.3      1.06      0.471\n2 baseline (-20%, defaults)        4.53  25.0      1.06      0.281\n```\n\n\n:::\n:::\n\n\n# The headline: two ways to make extremes worse {#sec-extremes}\n\n`exaggerate_extremes` amplifies the upper tail.\n`enforce_target_mean` rescales each (month, year) group so its wet-day mean hits\nthe target exactly.\nThey are usually described separately, but the second decides what the first\n*means*.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngrid_2x2 <- expand.grid(\n  exaggerate = c(FALSE, TRUE),\n  enforce    = c(TRUE, FALSE)\n)\n\ndo.call(bind_rows, lapply(seq_len(nrow(grid_2x2)), function(i) {\n  ex <- grid_2x2$exaggerate[i]\n  en <- grid_2x2$enforce[i]\n  summarise_series(\n    perturb(exaggerate_extremes = ex, enforce_target_mean = en, extreme_k = 1.5),\n    sprintf(\"exaggerate=%s, enforce=%s\", ex, en)\n  )\n}))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 5\n  scenario                        wetday_mean   p99 within_cv between_cv\n  <chr>                                 <dbl> <dbl>     <dbl>      <dbl>\n1 exaggerate=FALSE, enforce=TRUE         4.53  25.0      1.06      0.281\n2 exaggerate=TRUE, enforce=TRUE          4.53  31.6      1.17      0.281\n3 exaggerate=FALSE, enforce=FALSE        4.53  29.0      1.06      0.471\n4 exaggerate=TRUE, enforce=FALSE         5.12  45.9      1.17      0.554\n```\n\n\n:::\n:::\n\n\nThe target wet-day mean is 4.527 mm.\nThree of the four rows sit exactly on it.\nThe fourth — amplification with `enforce_target_mean = FALSE` — overshoots by\nabout 13%.\n\nThat is the whole interaction.\nAmplifying the tail moves probability mass upward, which *adds* water; the\nscenario then delivers a larger mean change than the one requested.\n`enforce_target_mean = TRUE` rescales the group back onto its target, so the\nsame amplification instead *redistributes* water: more in the tail, less in the\nbody, at the mean you asked for.\n\n::: {.callout-important}\n## Which one you want is a modelling choice\nRedistribution at a fixed mean is the usual intent for a stress test, because it\nkeeps the mean-change axis interpretable.\nIf you leave `enforce_target_mean = FALSE` while amplifying, the realized mean\nchange is no longer the number in your scenario table.\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\nexceedance <- function(x, label) {\n  v <- sort(wet_of(x), decreasing = TRUE)\n  tibble(value = v, prob = seq_along(v) / (length(v) + 1), scenario = label)\n}\n\nbind_rows(\n  exceedance(baseline, \"no amplification\"),\n  exceedance(perturb(exaggerate_extremes = TRUE, enforce_target_mean = TRUE,\n                     extreme_k = 1.5), \"amplified, mean enforced\"),\n  exceedance(perturb(exaggerate_extremes = TRUE, enforce_target_mean = FALSE,\n                     extreme_k = 1.5), \"amplified, mean free\")\n) |>\n  filter(prob < 0.3) |>\n  ggplot(aes(prob, value, colour = scenario)) +\n  geom_line(linewidth = 0.9) +\n  scale_x_log10(labels = scales::percent) +\n  scale_colour_viridis_d(end = 0.85) +\n  labs(\n    title = \"Enforcing the target mean turns amplification into redistribution\",\n    x = \"Exceedance probability\", y = \"Wet-day precipitation (mm)\",\n    colour = NULL\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![Wet-day exceedance curves. With the mean enforced, tail amplification pivots the distribution about its mean; without it, the whole curve shifts up.](perturbation_parameters_files/figure-html/fig-extremes-1.png){#fig-extremes fig-alt='Log-scaled exceedance probability curves for four combinations of exaggerate_extremes and enforce_target_mean, showing the unenforced amplified case lying above the others across the full range.' width=768}\n:::\n:::\n\n\n## How hard to amplify: `extreme_k` {#sec-extreme-k}\n\nWith the mean enforced, `extreme_k` is a clean dial.\nValues above 1 amplify, values below 1 dampen, and 1 is the identity.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nk_values <- c(0.6, 0.8, 1.0, 1.2, 1.5, 2.0, 2.5)\n\nk_sweep <- do.call(bind_rows, lapply(k_values, function(k) {\n  summarise_series(\n    perturb(exaggerate_extremes = TRUE, enforce_target_mean = TRUE, extreme_k = k),\n    sprintf(\"k = %.1f\", k)\n  )\n})) |>\n  mutate(k = k_values, .before = 1)\n\nk_sweep\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 7 × 6\n      k scenario wetday_mean   p99 within_cv between_cv\n  <dbl> <chr>          <dbl> <dbl>     <dbl>      <dbl>\n1   0.6 k = 0.6         4.53  20.0     0.963      0.281\n2   0.8 k = 0.8         4.53  21.8     1.01       0.281\n3   1   k = 1.0         4.53  25.0     1.06       0.281\n4   1.2 k = 1.2         4.53  27.9     1.10       0.281\n5   1.5 k = 1.5         4.53  31.6     1.17       0.281\n6   2   k = 2.0         4.53  35.2     1.25       0.281\n7   2.5 k = 2.5         4.53  39.4     1.32       0.281\n```\n\n\n:::\n:::\n\n\nThe wet-day mean does not move across the whole sweep, while the 99th percentile\nroughly doubles.\nThat is the response surface a stress test wants from a tail dimension: one\naxis, no side effects on the others.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nk_sweep |>\n  select(k, `99th percentile` = p99, `wet-day mean` = wetday_mean) |>\n  pivot_longer(-k) |>\n  ggplot(aes(k, value)) +\n  geom_line(linewidth = 0.9, colour = \"steelblue\") +\n  geom_point(size = 2, colour = \"steelblue\") +\n  facet_wrap(~name, scales = \"free_y\") +\n  expand_limits(y = 0) +\n  labs(\n    title = \"extreme_k moves the tail and leaves the mean alone\",\n    x = \"extreme_k\", y = \"mm/day\"\n  )\n```\n\n::: {.cell-output-display}\n![Tail severity against extreme_k, with the wet-day mean pinned by enforce_target_mean.](perturbation_parameters_files/figure-html/fig-k-sweep-1.png){#fig-k-sweep fig-alt='Two-panel plot: the 99th percentile rises steadily with extreme_k while the wet-day mean stays flat.' width=768}\n:::\n:::\n\n\n## Where the tail starts: `extreme_prob_threshold`\n\n`extreme_prob_threshold` sets the probability above which amplification applies.\nIts effect is less intuitive than `extreme_k`, and worth seeing before use.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nu0_values <- c(0.80, 0.90, 0.95, 0.99)\n\ntail_quantiles <- function(x, label) {\n  tibble(\n    scenario = label,\n    p90  = unname(quantile(wet_of(x), 0.90)),\n    p99  = unname(quantile(wet_of(x), 0.99)),\n    p999 = unname(quantile(wet_of(x), 0.999)),\n    wetday_mean = mean(wet_of(x))\n  )\n}\n\nbind_rows(\n  tail_quantiles(baseline, \"no amplification\"),\n  do.call(bind_rows, lapply(u0_values, function(u0) {\n    tail_quantiles(\n      perturb(exaggerate_extremes = TRUE, enforce_target_mean = TRUE,\n              extreme_k = 1.5, extreme_prob_threshold = u0),\n      sprintf(\"u0 = %.2f\", u0)\n    )\n  }))\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 5\n  scenario           p90   p99  p999 wetday_mean\n  <chr>            <dbl> <dbl> <dbl>       <dbl>\n1 no amplification  10.9  25.0  36.8        4.53\n2 u0 = 0.80         12.1  29.6  44.5        4.53\n3 u0 = 0.90         10.9  29.4  45.8        4.53\n4 u0 = 0.95         10.5  31.6  48.0        4.53\n5 u0 = 0.99         10.6  26.5  47.7        4.53\n```\n\n\n:::\n:::\n\n\nRead the columns, not the rows.\nLowering the threshold widens the affected region: at `u0 = 0.80` even the 90th\npercentile rises, because amplification has reached into the body of the\ndistribution.\nRaising it concentrates the effect — at `u0 = 0.99` the 90th and 99th\npercentiles are barely touched while the 99.9th still moves.\n\nThe 99th percentile is not monotone across this sweep, which looks odd until you\nnotice it sits *at* the threshold for the last row.\nWith the mean enforced, widening the amplified region also forces a larger\ncompensating rescale, so a given percentile can fall even as the far tail rises.\nPick the threshold to match the return period you care about and read the\nquantile that matches it, rather than treating `u0` as a severity dial —\nthat is `extreme_k`'s job.\n\n# Two knobs, two kinds of variability {#sec-two-knobs}\n\n`scale_var_with_mean` and `enforce_target_mean` both sound like they control\n\"variability\", and a single pooled coefficient of variation would make them look\nlike rivals.\nThey are not: they act on different things, and the two `*_cv` columns separate\nthem cleanly.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nknob_grid <- expand.grid(enforce = c(TRUE, FALSE), scale_var = c(TRUE, FALSE))\n\nbind_rows(\n  summarise_series(precip, \"observed\"),\n  do.call(bind_rows, lapply(seq_len(nrow(knob_grid)), function(i) {\n    en <- knob_grid$enforce[i]\n    sv <- knob_grid$scale_var[i]\n    summarise_series(\n      perturb(enforce_target_mean = en, scale_var_with_mean = sv),\n      sprintf(\"enforce=%s, scale_var=%s\", en, sv)\n    )\n  }))\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 5 × 5\n  scenario                       wetday_mean   p99 within_cv between_cv\n  <chr>                                <dbl> <dbl>     <dbl>      <dbl>\n1 observed                              5.66  36.3      1.06      0.471\n2 enforce=TRUE, scale_var=TRUE          4.53  25.0      1.06      0.281\n3 enforce=FALSE, scale_var=TRUE         4.53  29.0      1.06      0.471\n4 enforce=TRUE, scale_var=FALSE         4.53  30.3      1.29      0.281\n5 enforce=FALSE, scale_var=FALSE        4.51  37.3      1.29      0.560\n```\n\n\n:::\n:::\n\n\n`within_cv` responds only to `scale_var_with_mean`, and `between_cv` only to\n`enforce_target_mean`.\n\n**`scale_var_with_mean` preserves the shape of a wet day.**\nWhen `TRUE`, the effective variance factor becomes\n`var_factor * mean_factor^2`, so the standard deviation scales with the mean and\nthe coefficient of variation within a group is unchanged — it matches the\nobserved value exactly.\nWhen `FALSE`, a `var_factor` of 1 holds absolute variance while the mean drops\n20%, which inflates relative variability.\nWhether that is what you want depends on whether your scenario is \"the same\nweather, less of it\" or \"the same absolute spread around a lower mean\".\n\n**`enforce_target_mean` costs interannual variability.**\nPinning each group to `climatological mean x factor` means every year receives\nthe same target for a given month, so year-to-year differences in wet-day mean\nare largely erased: `between_cv` falls from the observed value to roughly half\nof it.\n\n::: {.callout-warning}\n## The default trades away the variability the generator produces\n`enforce_target_mean = TRUE` is the default, and it is the right default for\nhitting a scenario target exactly.\nBut interannual variability is precisely what the WARM component of this package\nexists to generate, and pinning every year to a climatological target discards a\nlarge part of it.\n\nIf your application is sensitive to sequences — multi-year drought, storage\ncarry-over, anything with memory — check `between_cv` on your own output before\naccepting the default, and consider `enforce_target_mean = FALSE` with the\nrealized mean change verified after the fact rather than imposed.\n:::\n\n\n::: {.cell}\n\n```{.r .cell-code}\nannual_wetmean <- function(x, label) {\n  tibble(\n    year = start_year + seq_len(n_years) - 1L,\n    value = vapply(split(x, year_idx), function(v) mean(v[v > 0]), numeric(1)),\n    scenario = label\n  )\n}\n\nbind_rows(\n  annual_wetmean(precip, \"observed\"),\n  annual_wetmean(perturb(enforce_target_mean = TRUE), \"enforce_target_mean = TRUE\"),\n  annual_wetmean(perturb(enforce_target_mean = FALSE), \"enforce_target_mean = FALSE\")\n) |>\n  ggplot(aes(year, value, colour = scenario)) +\n  geom_line(linewidth = 0.9) +\n  scale_colour_viridis_d(end = 0.85) +\n  labs(\n    title = \"Enforcing the target mean flattens year-to-year variability\",\n    x = NULL, y = \"Annual mean wet-day precipitation (mm)\", colour = NULL\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![Annual mean wet-day precipitation. Enforcing the target mean flattens the interannual signal the observed series carries.](perturbation_parameters_files/figure-html/fig-interannual-1.png){#fig-interannual fig-alt='Time series of annual mean wet-day precipitation over thirty years, showing the observed oscillation preserved when enforce_target_mean is FALSE and largely flattened when it is TRUE.' width=768}\n:::\n:::\n\n\n# Fitting: `qm_fit_method`\n\n`qm_fit_method` chooses how the monthly Gamma distributions are estimated —\n`\"mme\"` (moment matching, the default) or `\"mle\"`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfit_mme <- perturb(fit_method = \"mme\")\nfit_mle <- perturb(fit_method = \"mle\")\n\nbind_rows(\n  summarise_series(fit_mme, \"mme\"),\n  summarise_series(fit_mle, \"mle\")\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 5\n  scenario wetday_mean   p99 within_cv between_cv\n  <chr>          <dbl> <dbl>     <dbl>      <dbl>\n1 mme             4.53  25.0      1.06      0.281\n2 mle             4.53  25.0      1.06      0.281\n```\n\n\n:::\n:::\n\n\nThe two rows are identical at the printed precision, so it is worth looking at\nthe difference directly rather than at a rounded table:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntibble(\n  wetday_mean_difference = mean(wet_of(fit_mle)) - mean(wet_of(fit_mme)),\n  largest_daily_difference = max(abs(fit_mle - fit_mme)),\n  as_fraction_of_wetday_mean =\n    max(abs(fit_mle - fit_mme)) / mean(wet_of(fit_mme))\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 1 × 3\n  wetday_mean_difference largest_daily_difference as_fraction_of_wetday_mean\n                   <dbl>                    <dbl>                      <dbl>\n1               0.000429                  0.00911                    0.00201\n```\n\n\n:::\n:::\n\n\nThe wet-day means differ in the fourth decimal, and the largest single-day\ndifference is a small fraction of a typical wet day.\nThat is the honest result: on a well-behaved Gamma sample the two estimators\neffectively coincide.\nThis is a robustness setting, not a scenario dimension — it earns attention when\nfitting is hard (short records, months with few wet days, heavily skewed\nsamples), not as something to vary across a stress-test ensemble.\n`min_events`, which controls how many wet days a month needs before it is fitted\nat all, matters more often.\n\n# Transient versus step change {#sec-transient}\n\nThe `*_transient` arguments live on `apply_climate_perturbations()`, so this\nsection uses the wrapper.\nThat needs grid metadata and temperature columns, which the mapper did not.\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngrid <- data.frame(id = 1L, lat = 0)\n\nset.seed(7)\ncell <- data.frame(\n  precip   = precip,\n  temp     = rnorm(length(date), 25, 3),\n  temp_min = rnorm(length(date), 20, 3)\n)\ncell$temp_max <- pmax(cell$temp_min + 5, rnorm(length(date), 30, 3))\n```\n:::\n\n\nA transient factor is specified by its **end state**, not by a per-year path.\n`transient = TRUE` ramps linearly from 1 to `2f - 1`, so the ramp averages to\n`f` over the period.\nA request for `f = 0.7` therefore means \"ending 40% down, averaging 30% down\",\nnot \"30% down every year\".\n\nOccurrence shows this most clearly, because wet-day frequency is not touched by\nthe mean-enforcement machinery above.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nwet_fraction_by_year <- function(x) {\n  vapply(split(x, year_idx), function(v) mean(v > 0), numeric(1))\n}\nobserved_fraction <- wet_fraction_by_year(precip)\n\nrun_occurrence <- function(transient) {\n  out <- apply_climate_perturbations(\n    data = list(cell), grid = grid, date = date,\n    precip_mean_factor       = rep(1, 12),\n    precip_occurrence_factor = rep(0.7, 12),\n    temp_delta               = rep(0, 12),\n    precip_transient             = FALSE,\n    precip_occurrence_transient  = transient,\n    temp_transient               = FALSE,\n    compute_pet = FALSE, diagnostic = FALSE, verbose = FALSE, seed = 1L\n  )\n  tibble(\n    year = start_year + seq_len(n_years) - 1L,\n    ratio = as.numeric(wet_fraction_by_year(out[[1]]$precip) / observed_fraction),\n    scenario = if (transient) \"transient (ramp)\" else \"step change\"\n  )\n}\n\noccurrence_paths <- bind_rows(run_occurrence(FALSE), run_occurrence(TRUE))\n\noccurrence_paths |>\n  group_by(scenario) |>\n  summarise(\n    first_year = first(ratio), last_year = last(ratio), period_mean = mean(ratio)\n  )\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 4\n  scenario         first_year last_year period_mean\n  <chr>                 <dbl>     <dbl>       <dbl>\n1 step change           0.688     0.697       0.696\n2 transient (ramp)      1         0.394       0.705\n```\n\n\n:::\n:::\n\n\nBoth average to the requested 0.7.\nThe step change holds it flat; the ramp starts at no change and ends near 0.4.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(occurrence_paths, aes(year, ratio, colour = scenario)) +\n  geom_line(linewidth = 0.9) +\n  geom_hline(yintercept = 0.7, linetype = \"dashed\", colour = \"grey40\") +\n  scale_colour_viridis_d(end = 0.7) +\n  expand_limits(y = 0) +\n  labs(\n    title = \"Same requested factor, two different paths\",\n    subtitle = \"Dashed line marks the requested 0.7\",\n    x = NULL, y = \"Wet-day frequency / observed\", colour = NULL\n  ) +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![Wet-day frequency relative to observed, for a step change and a transient ramp requesting the same 0.7 factor.](perturbation_parameters_files/figure-html/fig-transient-1.png){#fig-transient fig-alt='Two time series of the wet-day frequency ratio over thirty years: a flat line near 0.7 for the step change, and a declining line from 1.0 to about 0.4 for the transient ramp.' width=768}\n:::\n:::\n\n\n## A transient factor takes a vector, not a year-varying matrix\n\nBecause the ramp is built from the end state, only the first row of a factor is\nread in transient mode.\nSupplying a year-varying `n_years x 12` matrix *and* setting the transient flag\nis therefore a contradiction, and is an error rather than a silent truncation:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nvarying <- matrix(0.5, nrow = n_years, ncol = 12)\nvarying[1, ] <- 1.0\n\napply_climate_perturbations(\n  data = list(cell), grid = grid, date = date,\n  precip_mean_factor = varying,\n  temp_delta = rep(0, 12),\n  precip_transient = TRUE,\n  compute_pet = FALSE, diagnostic = FALSE, verbose = FALSE\n)\n```\n\n::: {.cell-output .cell-output-error}\n\n```\nError:\n! 'precip_mean_factor' varies by year, but its transient flag is TRUE. A transient factor ramps from 1 to 2f-1 using row 1 alone, so rows 2:30 would be discarded. Supply a length-12 vector (the end-state change, whose ramp averages to it), or set the transient flag to FALSE to apply the matrix year by year.\n```\n\n\n:::\n:::\n\n\nPass a length-12 vector and let the ramp build the path, or set the transient\nflag to `FALSE` to have the matrix applied year by year.\nA matrix whose rows are all identical is equivalent to a vector, so code that\nexpanded a vector to a constant matrix is unaffected.\n\n## Intensity transients interact with the enforced mean\n\nThe same ramp applied to intensity is harder to read, and the reason is\n@sec-two-knobs.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nobserved_wetmean <- vapply(split(precip, year_idx),\n                           function(v) mean(v[v > 0]), numeric(1))\n\nrun_intensity <- function(transient, enforce) {\n  out <- apply_climate_perturbations(\n    data = list(cell), grid = grid, date = date,\n    precip_mean_factor  = rep(0.7, 12),\n    temp_delta          = rep(0, 12),\n    precip_transient    = transient,\n    temp_transient      = FALSE,\n    enforce_target_mean = enforce,\n    compute_pet = FALSE, diagnostic = FALSE, verbose = FALSE, seed = 1L\n  )\n  realised <- vapply(split(out[[1]]$precip, year_idx),\n                     function(v) mean(v[v > 0]), numeric(1))\n  tibble(\n    scenario = sprintf(\"transient=%s, enforce=%s\", transient, enforce),\n    year_1   = unname(realised / observed_wetmean)[1],\n    year_30  = unname(realised / observed_wetmean)[n_years],\n    period_mean = mean(realised / observed_wetmean)\n  )\n}\n\nbind_rows(\n  run_intensity(FALSE, TRUE), run_intensity(TRUE, TRUE),\n  run_intensity(FALSE, FALSE), run_intensity(TRUE, FALSE)\n)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 4 × 4\n  scenario                       year_1 year_30 period_mean\n  <chr>                           <dbl>   <dbl>       <dbl>\n1 transient=FALSE, enforce=TRUE   0.698   1.17        0.814\n2 transient=TRUE, enforce=TRUE    0.997   0.666       0.803\n3 transient=FALSE, enforce=FALSE  0.7     0.7         0.7  \n4 transient=TRUE, enforce=FALSE   1       0.597       0.714\n```\n\n\n:::\n:::\n\n\nWith `enforce_target_mean = FALSE` the step change sits at exactly 0.70 in every\nyear, and the ramp tracks the intended path.\nWith it `TRUE`, neither ratio matches: each group is pinned to\n`climatological mean x factor`, so the ratio against *that year's* observed mean\nis above the factor in dry years and below it in wet ones.\n\nNeither is wrong — they are different definitions of the target.\n`enforce_target_mean = TRUE` means \"the wet-day mean shall be 70% of\nclimatology\"; `FALSE` means \"each year gets 70% of whatever it had\".\nFor a transient scenario, where the point is a path through time, the second is\nusually the intended reading.\n\n# Choosing settings\n\nA summary of what the article establishes, rather than new results.\n\n| Argument | Controls | Reach for it when |\n|---|---|---|\n| `exaggerate_extremes`, `extreme_k` | Tail severity at fixed mean | Extremes are a stress dimension of their own |\n| `extreme_prob_threshold` | How much of the distribution counts as tail | The return period of interest is known |\n| `enforce_target_mean` | Whether the mean is exact or emergent | `TRUE` to hit a target; `FALSE` to keep interannual structure |\n| `scale_var_with_mean` | Wet-day shape under a mean change | `TRUE` for \"same weather, less of it\" |\n| `qm_fit_method` | Gamma estimation | Fitting is hard; not a scenario axis |\n| `precip_transient`, `precip_occurrence_transient` | Step change versus ramp to an end state | The scenario is a path, not a level |\n\nTwo defaults are worth a deliberate decision rather than acceptance.\n`enforce_target_mean = TRUE` buys exact targeting at the cost of interannual\nvariability, which is the thing the generator upstream exists to produce.\nAnd `scale_var_with_mean = TRUE` is a statement that a drier climate has\nproportionally similar wet days — defensible, and still a statement.\n\n# Session info\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n```\nR version 4.6.0 (2026-04-24 ucrt)\nPlatform: x86_64-w64-mingw32/x64\nRunning under: Windows 11 x64 (build 26200)\n\nMatrix products: default\n  LAPACK version 3.12.1\n\nlocale:\n[1] LC_COLLATE=English_United Kingdom.utf8 \n[2] LC_CTYPE=English_United Kingdom.utf8   \n[3] LC_MONETARY=English_United Kingdom.utf8\n[4] LC_NUMERIC=C                           \n[5] LC_TIME=English_United Kingdom.utf8    \n\ntime zone: Europe/Amsterdam\ntzcode source: internal\n\nattached base packages:\n[1] stats     graphics  grDevices utils     datasets  methods   base     \n\nother attached packages:\n[1] tidyr_1.3.2       dplyr_1.2.1       ggplot2_4.0.3     weathergenr_2.0.0\n\nloaded via a namespace (and not attached):\n [1] utf8_1.2.6         generics_0.1.4     class_7.3-23       lattice_0.22-9    \n [5] digest_0.6.39      magrittr_2.0.5     evaluate_1.0.5     grid_4.6.0        \n [9] RColorBrewer_1.1-3 waveslim_1.8.5     multitaper_1.0-17  iterators_1.0.14  \n[13] fastmap_1.2.0      foreach_1.5.2      jsonlite_2.0.0     Matrix_1.7-5      \n[17] e1071_1.7-17       survival_3.8-6     purrr_1.2.2        viridisLite_0.4.3 \n[21] scales_1.4.0       codetools_0.2-20   cli_3.6.6          rlang_1.3.0       \n[25] splines_4.6.0      withr_3.0.3        yaml_2.3.12        otel_0.2.0        \n[29] tools_4.6.0        ncdf4_1.24         fitdistrplus_1.2-6 vctrs_0.7.3       \n[33] R6_2.6.1           proxy_0.4-29       lifecycle_1.0.5    MASS_7.3-65       \n[37] pkgconfig_2.0.3    pillar_1.11.1      gtable_0.3.6       glue_1.8.1        \n[41] xfun_0.60          tibble_3.3.1       tidyselect_1.2.1   rstudioapi_0.19.0 \n[45] knitr_1.51         farver_2.1.2       htmltools_0.5.9    patchwork_1.3.2   \n[49] labeling_0.4.3     rmarkdown_2.31     compiler_4.6.0     S7_0.2.2          \n```\n\n\n:::\n:::\n\n",
    "supporting": [
      "perturbation_parameters_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}